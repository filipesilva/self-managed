import { TimeNumberPipe } from './time-number.pipe';

describe('TimeNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
